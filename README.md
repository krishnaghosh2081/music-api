# music-api

This api store the user records, user recorded file information , songs etc...Uploads the requested audio files to cloud storage. Keep daily log etc...
Also there is a functionality to trigger the stem analysis through Kafka as a producer.

Add .env file and add PORT and MONGODB_URI, OLLAMA_API_KEY properties