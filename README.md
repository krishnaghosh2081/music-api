# music-api

This api stores the user records, user recorded file information , songs etc...Uploads the requested audio files to cloud storage. Keep daily log etc...
Also there is a functionality to trigger the stem analysis through Kafka as a producer.

Add .env file and add PORT and MONGODB_URI, OLLAMA_API_KEY, CLOUD_NAME, CLOUDINARY_KEY, CLOUDINARY_SECRET, KAFKA_BROKER properties